[project]
name = "bookmark-manager"
version = "0.1.0"
description = "Add your description here"
readme = "../README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.111.0",
    "httpx>=0.27.0",
    "hypothesis>=6.100.0",
    "pydantic>=2.7.0",
    "pytest>=8.2.0",
    "uvicorn[standard]>=0.29.0",
]

[project.scripts]
api-dev = "api.main:main"
api-run = "api.main:main"
